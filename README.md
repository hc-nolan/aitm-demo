# Instructions

Start by cloning the repository: `git clone https://github.com/hc-nolan/aitm-demo`
- git

Install
- [Docker Engine](https://docs.docker.com/engine/install/)
- Golang (`sudo apt install -y golang-go`)

Run `./start.sh`
